/**
 * Is not the number 0
 * 
 * given any element, return true if it is not the nuber 0
 * isNotZero(5) -> true
 * isNotZero(0) -> false
 * isNotZero(null) -> true
 */

function isNotZero(element) {
    return element !== 0
}

console.log(isNotZero(null))