/**Return the sum of two Numbers 
 * Create a function that takes i 2 arguments as functions and return their sum
 * 
 * addition (3, 5)
*/

function sumOfNums (numOne, numTwo) {
    return numOne + numTwo          // have to return if you want to console log it
}   
console.log(sumOfNums(10, 7))