/**
 * Is the number odd
 * given two numbers, return true if the number is odd
 * isOdd(1) = true
 * isOdd(2) = false
 * isOdd(3) = true
 */

function isOdd(num) {
    return num % 2 !==0
}

console.log(isOdd(14))